naftiko: 1.0.0-alpha2
info:
  label: Connector API — Preauthorizations
  description: 'Connector API — Preauthorizations. 1 operations. Lead operation: Get all preauthorizations by customers. Self-contained
    Naftiko capability covering one Mews business surface.'
  tags:
  - Mews
  - Preauthorizations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MEWS_API_KEY: MEWS_API_KEY
capability:
  consumes:
  - type: http
    namespace: connector-preauthorizations
    baseUri: https://api.mews.com
    description: Connector API — Preauthorizations business capability. Self-contained, no shared references.
    resources:
    - name: api-connector-v1-preauthorizations-getAllByCustomers
      path: /api/connector/v1/preauthorizations/getAllByCustomers
      operations:
      - name: preauthorizationsgetallbycustomers
        method: POST
        description: Get all preauthorizations by customers
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: connector-preauthorizations-rest
    port: 8080
    description: REST adapter for Connector API — Preauthorizations. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/connector/v1/preauthorizations/getallbycustomers
      name: api-connector-v1-preauthorizations-getallbycustomers
      description: REST surface for api-connector-v1-preauthorizations-getAllByCustomers.
      operations:
      - method: POST
        name: preauthorizationsgetallbycustomers
        description: Get all preauthorizations by customers
        call: connector-preauthorizations.preauthorizationsgetallbycustomers
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: connector-preauthorizations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Connector API — Preauthorizations. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: get-all-preauthorizations-customers
      description: Get all preauthorizations by customers
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: connector-preauthorizations.preauthorizationsgetallbycustomers
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
