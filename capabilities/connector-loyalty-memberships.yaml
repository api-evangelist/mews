naftiko: 1.0.0-alpha2
info:
  label: Connector API — Loyalty memberships
  description: 'Connector API — Loyalty memberships. 4 operations. Lead operation: Add loyalty memberships. Self-contained
    Naftiko capability covering one Mews business surface.'
  tags:
  - Mews
  - Loyalty memberships
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MEWS_API_KEY: MEWS_API_KEY
capability:
  consumes:
  - type: http
    namespace: connector-loyalty-memberships
    baseUri: https://api.mews.com
    description: Connector API — Loyalty memberships business capability. Self-contained, no shared references.
    resources:
    - name: api-connector-v1-loyaltyMemberships-add
      path: /api/connector/v1/loyaltyMemberships/add
      operations:
      - name: loyaltymembershipsadd
        method: POST
        description: Add loyalty memberships
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: api-connector-v1-loyaltyMemberships-delete
      path: /api/connector/v1/loyaltyMemberships/delete
      operations:
      - name: loyaltymembershipsdelete
        method: POST
        description: Delete loyalty memberships
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: api-connector-v1-loyaltyMemberships-getAll
      path: /api/connector/v1/loyaltyMemberships/getAll
      operations:
      - name: loyaltymembershipsgetall
        method: POST
        description: Get all loyalty memberships
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: api-connector-v1-loyaltyMemberships-update
      path: /api/connector/v1/loyaltyMemberships/update
      operations:
      - name: loyaltymembershipsupdate
        method: POST
        description: Update loyalty memberships
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: connector-loyalty-memberships-rest
    port: 8080
    description: REST adapter for Connector API — Loyalty memberships. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/connector/v1/loyaltymemberships/add
      name: api-connector-v1-loyaltymemberships-add
      description: REST surface for api-connector-v1-loyaltyMemberships-add.
      operations:
      - method: POST
        name: loyaltymembershipsadd
        description: Add loyalty memberships
        call: connector-loyalty-memberships.loyaltymembershipsadd
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/connector/v1/loyaltymemberships/delete
      name: api-connector-v1-loyaltymemberships-delete
      description: REST surface for api-connector-v1-loyaltyMemberships-delete.
      operations:
      - method: POST
        name: loyaltymembershipsdelete
        description: Delete loyalty memberships
        call: connector-loyalty-memberships.loyaltymembershipsdelete
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/connector/v1/loyaltymemberships/getall
      name: api-connector-v1-loyaltymemberships-getall
      description: REST surface for api-connector-v1-loyaltyMemberships-getAll.
      operations:
      - method: POST
        name: loyaltymembershipsgetall
        description: Get all loyalty memberships
        call: connector-loyalty-memberships.loyaltymembershipsgetall
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/connector/v1/loyaltymemberships/update
      name: api-connector-v1-loyaltymemberships-update
      description: REST surface for api-connector-v1-loyaltyMemberships-update.
      operations:
      - method: POST
        name: loyaltymembershipsupdate
        description: Update loyalty memberships
        call: connector-loyalty-memberships.loyaltymembershipsupdate
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: connector-loyalty-memberships-mcp
    port: 9090
    transport: http
    description: MCP adapter for Connector API — Loyalty memberships. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: add-loyalty-memberships
      description: Add loyalty memberships
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: connector-loyalty-memberships.loyaltymembershipsadd
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-loyalty-memberships
      description: Delete loyalty memberships
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: connector-loyalty-memberships.loyaltymembershipsdelete
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: get-all-loyalty-memberships
      description: Get all loyalty memberships
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: connector-loyalty-memberships.loyaltymembershipsgetall
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: update-loyalty-memberships
      description: Update loyalty memberships
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: connector-loyalty-memberships.loyaltymembershipsupdate
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
