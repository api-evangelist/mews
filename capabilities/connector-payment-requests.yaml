naftiko: 1.0.0-alpha2
info:
  label: Connector API — Payment requests
  description: 'Connector API — Payment requests. 3 operations. Lead operation: Add payment requests. Self-contained Naftiko
    capability covering one Mews business surface.'
  tags:
  - Mews
  - Payment requests
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MEWS_API_KEY: MEWS_API_KEY
capability:
  consumes:
  - type: http
    namespace: connector-payment-requests
    baseUri: https://api.mews.com
    description: Connector API — Payment requests business capability. Self-contained, no shared references.
    resources:
    - name: api-connector-v1-paymentRequests-add
      path: /api/connector/v1/paymentRequests/add
      operations:
      - name: paymentrequestsadd
        method: POST
        description: Add payment requests
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: api-connector-v1-paymentRequests-cancel
      path: /api/connector/v1/paymentRequests/cancel
      operations:
      - name: paymentrequestscancel
        method: POST
        description: Cancel payment requests
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: api-connector-v1-paymentRequests-getAll
      path: /api/connector/v1/paymentRequests/getAll
      operations:
      - name: paymentrequestsgetall
        method: POST
        description: Get all payment requests
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: connector-payment-requests-rest
    port: 8080
    description: REST adapter for Connector API — Payment requests. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/connector/v1/paymentrequests/add
      name: api-connector-v1-paymentrequests-add
      description: REST surface for api-connector-v1-paymentRequests-add.
      operations:
      - method: POST
        name: paymentrequestsadd
        description: Add payment requests
        call: connector-payment-requests.paymentrequestsadd
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/connector/v1/paymentrequests/cancel
      name: api-connector-v1-paymentrequests-cancel
      description: REST surface for api-connector-v1-paymentRequests-cancel.
      operations:
      - method: POST
        name: paymentrequestscancel
        description: Cancel payment requests
        call: connector-payment-requests.paymentrequestscancel
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/connector/v1/paymentrequests/getall
      name: api-connector-v1-paymentrequests-getall
      description: REST surface for api-connector-v1-paymentRequests-getAll.
      operations:
      - method: POST
        name: paymentrequestsgetall
        description: Get all payment requests
        call: connector-payment-requests.paymentrequestsgetall
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: connector-payment-requests-mcp
    port: 9090
    transport: http
    description: MCP adapter for Connector API — Payment requests. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: add-payment-requests
      description: Add payment requests
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: connector-payment-requests.paymentrequestsadd
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: cancel-payment-requests
      description: Cancel payment requests
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: connector-payment-requests.paymentrequestscancel
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: get-all-payment-requests
      description: Get all payment requests
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: connector-payment-requests.paymentrequestsgetall
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
