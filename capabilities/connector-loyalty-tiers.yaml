naftiko: 1.0.0-alpha2
info:
  label: Connector API — Loyalty tiers
  description: 'Connector API — Loyalty tiers. 4 operations. Lead operation: Add loyalty tiers. Self-contained Naftiko capability
    covering one Mews business surface.'
  tags:
  - Mews
  - Loyalty tiers
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MEWS_API_KEY: MEWS_API_KEY
capability:
  consumes:
  - type: http
    namespace: connector-loyalty-tiers
    baseUri: https://api.mews.com
    description: Connector API — Loyalty tiers business capability. Self-contained, no shared references.
    resources:
    - name: api-connector-v1-loyaltyTiers-add
      path: /api/connector/v1/loyaltyTiers/add
      operations:
      - name: loyaltytiersadd
        method: POST
        description: Add loyalty tiers
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: api-connector-v1-loyaltyTiers-delete
      path: /api/connector/v1/loyaltyTiers/delete
      operations:
      - name: loyaltytiersdelete
        method: POST
        description: Delete loyalty tiers
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: api-connector-v1-loyaltyTiers-getAll
      path: /api/connector/v1/loyaltyTiers/getAll
      operations:
      - name: loyaltytiersgetall
        method: POST
        description: Get all loyalty tiers
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: api-connector-v1-loyaltyTiers-update
      path: /api/connector/v1/loyaltyTiers/update
      operations:
      - name: loyaltytiersupdate
        method: POST
        description: Update loyalty tiers
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: connector-loyalty-tiers-rest
    port: 8080
    description: REST adapter for Connector API — Loyalty tiers. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/api/connector/v1/loyaltytiers/add
      name: api-connector-v1-loyaltytiers-add
      description: REST surface for api-connector-v1-loyaltyTiers-add.
      operations:
      - method: POST
        name: loyaltytiersadd
        description: Add loyalty tiers
        call: connector-loyalty-tiers.loyaltytiersadd
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/connector/v1/loyaltytiers/delete
      name: api-connector-v1-loyaltytiers-delete
      description: REST surface for api-connector-v1-loyaltyTiers-delete.
      operations:
      - method: POST
        name: loyaltytiersdelete
        description: Delete loyalty tiers
        call: connector-loyalty-tiers.loyaltytiersdelete
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/connector/v1/loyaltytiers/getall
      name: api-connector-v1-loyaltytiers-getall
      description: REST surface for api-connector-v1-loyaltyTiers-getAll.
      operations:
      - method: POST
        name: loyaltytiersgetall
        description: Get all loyalty tiers
        call: connector-loyalty-tiers.loyaltytiersgetall
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/connector/v1/loyaltytiers/update
      name: api-connector-v1-loyaltytiers-update
      description: REST surface for api-connector-v1-loyaltyTiers-update.
      operations:
      - method: POST
        name: loyaltytiersupdate
        description: Update loyalty tiers
        call: connector-loyalty-tiers.loyaltytiersupdate
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: connector-loyalty-tiers-mcp
    port: 9090
    transport: http
    description: MCP adapter for Connector API — Loyalty tiers. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: add-loyalty-tiers
      description: Add loyalty tiers
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: connector-loyalty-tiers.loyaltytiersadd
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-loyalty-tiers
      description: Delete loyalty tiers
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: connector-loyalty-tiers.loyaltytiersdelete
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: get-all-loyalty-tiers
      description: Get all loyalty tiers
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: connector-loyalty-tiers.loyaltytiersgetall
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: update-loyalty-tiers
      description: Update loyalty tiers
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: connector-loyalty-tiers.loyaltytiersupdate
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
