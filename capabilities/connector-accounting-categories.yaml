naftiko: 1.0.0-alpha2
info:
  label: Connector API — Accounting categories
  description: 'Connector API — Accounting categories. 1 operations. Lead operation: Get all accounting categories. Self-contained
    Naftiko capability covering one Mews business surface.'
  tags:
  - Mews
  - Accounting categories
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MEWS_API_KEY: MEWS_API_KEY
capability:
  consumes:
  - type: http
    namespace: connector-accounting-categories
    baseUri: https://api.mews.com
    description: Connector API — Accounting categories business capability. Self-contained, no shared references.
    resources:
    - name: api-connector-v1-accountingCategories-getAll
      path: /api/connector/v1/accountingCategories/getAll
      operations:
      - name: accountingcategoriesgetall
        method: POST
        description: Get all accounting categories
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: connector-accounting-categories-rest
    port: 8080
    description: REST adapter for Connector API — Accounting categories. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/connector/v1/accountingcategories/getall
      name: api-connector-v1-accountingcategories-getall
      description: REST surface for api-connector-v1-accountingCategories-getAll.
      operations:
      - method: POST
        name: accountingcategoriesgetall
        description: Get all accounting categories
        call: connector-accounting-categories.accountingcategoriesgetall
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: connector-accounting-categories-mcp
    port: 9090
    transport: http
    description: MCP adapter for Connector API — Accounting categories. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: get-all-accounting-categories
      description: Get all accounting categories
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: connector-accounting-categories.accountingcategoriesgetall
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
