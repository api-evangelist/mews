naftiko: 1.0.0-alpha2
info:
  label: Connector API — Voucher codes
  description: 'Connector API — Voucher codes. 3 operations. Lead operation: Add voucher codes. Self-contained Naftiko capability
    covering one Mews business surface.'
  tags:
  - Mews
  - Voucher codes
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MEWS_API_KEY: MEWS_API_KEY
capability:
  consumes:
  - type: http
    namespace: connector-voucher-codes
    baseUri: https://api.mews.com
    description: Connector API — Voucher codes business capability. Self-contained, no shared references.
    resources:
    - name: api-connector-v1-voucherCodes-add
      path: /api/connector/v1/voucherCodes/add
      operations:
      - name: vouchercodesadd
        method: POST
        description: Add voucher codes
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: api-connector-v1-voucherCodes-delete
      path: /api/connector/v1/voucherCodes/delete
      operations:
      - name: vouchercodesdelete
        method: POST
        description: Delete voucher codes
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: api-connector-v1-voucherCodes-getAll
      path: /api/connector/v1/voucherCodes/getAll
      operations:
      - name: vouchercodesgetall
        method: POST
        description: Get all voucher codes
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: connector-voucher-codes-rest
    port: 8080
    description: REST adapter for Connector API — Voucher codes. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/api/connector/v1/vouchercodes/add
      name: api-connector-v1-vouchercodes-add
      description: REST surface for api-connector-v1-voucherCodes-add.
      operations:
      - method: POST
        name: vouchercodesadd
        description: Add voucher codes
        call: connector-voucher-codes.vouchercodesadd
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/connector/v1/vouchercodes/delete
      name: api-connector-v1-vouchercodes-delete
      description: REST surface for api-connector-v1-voucherCodes-delete.
      operations:
      - method: POST
        name: vouchercodesdelete
        description: Delete voucher codes
        call: connector-voucher-codes.vouchercodesdelete
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/connector/v1/vouchercodes/getall
      name: api-connector-v1-vouchercodes-getall
      description: REST surface for api-connector-v1-voucherCodes-getAll.
      operations:
      - method: POST
        name: vouchercodesgetall
        description: Get all voucher codes
        call: connector-voucher-codes.vouchercodesgetall
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: connector-voucher-codes-mcp
    port: 9090
    transport: http
    description: MCP adapter for Connector API — Voucher codes. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: add-voucher-codes
      description: Add voucher codes
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: connector-voucher-codes.vouchercodesadd
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-voucher-codes
      description: Delete voucher codes
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: connector-voucher-codes.vouchercodesdelete
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: get-all-voucher-codes
      description: Get all voucher codes
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: connector-voucher-codes.vouchercodesgetall
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
