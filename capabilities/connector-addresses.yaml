naftiko: 1.0.0-alpha2
info:
  label: Connector API — Addresses
  description: 'Connector API — Addresses. 4 operations. Lead operation: Add addresses. Self-contained Naftiko capability
    covering one Mews business surface.'
  tags:
  - Mews
  - Addresses
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MEWS_API_KEY: MEWS_API_KEY
capability:
  consumes:
  - type: http
    namespace: connector-addresses
    baseUri: https://api.mews.com
    description: Connector API — Addresses business capability. Self-contained, no shared references.
    resources:
    - name: api-connector-v1-addresses-add
      path: /api/connector/v1/addresses/add
      operations:
      - name: addressesadd
        method: POST
        description: Add addresses
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: api-connector-v1-addresses-delete
      path: /api/connector/v1/addresses/delete
      operations:
      - name: addressesdelete
        method: POST
        description: Delete addresses
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: api-connector-v1-addresses-getAll
      path: /api/connector/v1/addresses/getAll
      operations:
      - name: addressesgetall
        method: POST
        description: Get all addresses
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: api-connector-v1-addresses-update
      path: /api/connector/v1/addresses/update
      operations:
      - name: addressesupdate
        method: POST
        description: Update addresses
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
  exposes:
  - type: rest
    namespace: connector-addresses-rest
    port: 8080
    description: REST adapter for Connector API — Addresses. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/api/connector/v1/addresses/add
      name: api-connector-v1-addresses-add
      description: REST surface for api-connector-v1-addresses-add.
      operations:
      - method: POST
        name: addressesadd
        description: Add addresses
        call: connector-addresses.addressesadd
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/connector/v1/addresses/delete
      name: api-connector-v1-addresses-delete
      description: REST surface for api-connector-v1-addresses-delete.
      operations:
      - method: POST
        name: addressesdelete
        description: Delete addresses
        call: connector-addresses.addressesdelete
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/connector/v1/addresses/getall
      name: api-connector-v1-addresses-getall
      description: REST surface for api-connector-v1-addresses-getAll.
      operations:
      - method: POST
        name: addressesgetall
        description: Get all addresses
        call: connector-addresses.addressesgetall
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/connector/v1/addresses/update
      name: api-connector-v1-addresses-update
      description: REST surface for api-connector-v1-addresses-update.
      operations:
      - method: POST
        name: addressesupdate
        description: Update addresses
        call: connector-addresses.addressesupdate
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: connector-addresses-mcp
    port: 9090
    transport: http
    description: MCP adapter for Connector API — Addresses. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: add-addresses
      description: Add addresses
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: connector-addresses.addressesadd
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-addresses
      description: Delete addresses
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: connector-addresses.addressesdelete
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: get-all-addresses
      description: Get all addresses
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: connector-addresses.addressesgetall
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: update-addresses
      description: Update addresses
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: connector-addresses.addressesupdate
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
