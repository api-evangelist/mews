{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/ReservationsChannelManagerDetailsFilterParameters",
  "title": "Reservation channel manager details filter parameters",
  "required": [
    "AccessToken",
    "Client",
    "ClientToken",
    "ReservationIds"
  ],
  "type": "object",
  "properties": {
    "ClientToken": {
      "minLength": 1,
      "type": "string",
      "description": "Token identifying the client application."
    },
    "AccessToken": {
      "minLength": 1,
      "type": "string",
      "description": "Access token of the client application."
    },
    "Client": {
      "minLength": 1,
      "type": "string",
      "description": "Name and version of the client application."
    },
    "ReservationIds": {
      "maxItems": 100,
      "minItems": 1,
      "type": "array",
      "items": {
        "type": "string",
        "format": "uuid"
      },
      "description": "Unique identifiers of the `Reservations`."
    }
  },
  "additionalProperties": false,
  "x-schema-id": "ReservationsChannelManagerDetailsFilterParameters"
}